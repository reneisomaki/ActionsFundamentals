name: CICD Package API

on:
  push:
    branches: [ main ]
    paths: 
      - 'packages/api/**'
      - 'packages/shared/**'
  workflow_dispatch:

env:
  PACKAGE: api
    
jobs:
  Build-Test-Deploy:
    uses: ./.github/workflows/cicd_build-test-deploy.yml
    with:
      package: api
