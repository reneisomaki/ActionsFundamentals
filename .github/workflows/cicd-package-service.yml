name: CICD Package Service

on:
  push:
    branches: [ main ]
    paths: 
      - 'packages/service/**'

  workflow_dispatch:

jobs:
  Package:
    uses: ./.github/workflows/cicd_build-test-deploy.yml
    with:
      package: service
    